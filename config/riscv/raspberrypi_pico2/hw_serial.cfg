/*
 *	SIOドライバ（Raspberry Pi Pico2用）のコンフィギュレーションファイル
 */
#include <hw_serial.h>
INCLUDE("\"hw_serial.h\"");

DEF_INH( INHNO_SIO1, { TA_HLNG, sio_handler } );
#if TNUM_SIOP >= 2
DEF_INH( INHNO_SIO2, { TA_HLNG, sio_handler2 } );
#endif
